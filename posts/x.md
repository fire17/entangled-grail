# X thread draft (DO NOT POST without user go)

1/ You've seen this: two browser windows, two particle orbs, one tether. The famous repo
   only has cubes. The REAL code was never on GitHub — so I pulled it off the blockchain. 🧵
   [attach images/03-grail-green-red.png]

2/ It's "Entangled" by @_nonfigurativ_ — released as an fxhash VERTEX piece, stored on
   onchfs (files literally on-chain). Extracted the generator, byte-for-byte.

3/ Fun part: its style RNG is hash-independent (WASM "squares", fixed seeds). Replicated it
   offline → mapped all 256 editions → found the EXACT two from the viral video: iteration
   253 (green/red) + 58 (red/green).

4/ Built around it: a launcher for every recovered version + a live cycler (◀▶ steps
   editions exactly, autoplay w/ dwell slider, live palette preview that reskins the same
   million particles mid-flight). [attach images/04-iteration-cycler.png]

5/ All artwork © Bjørn Staal — provenance + takedown offer in NOTICE.md.
   github.com/fire17/entangled-grail
   (placeholder: HN link — post HN first, then link here)

Posting notes: HN first (morning US), then thread; first HN comment = the recovery technique detail.
